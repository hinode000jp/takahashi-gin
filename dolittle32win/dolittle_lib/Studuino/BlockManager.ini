BlockManager=配列！作る。
BlockManager:Current=""。
BlockManager:get=「|info ;ret|
	info = Current.
info	
」。

BlockManager:Up=「|;ret|
	self！（Current)　書く。
	Current =Current!作る。
Current
」。

BlockManager:Down=「|;ret|
	Current=self！（self!要素数？)　読む。
	self!（self!要素数？)  位置で消す。
Current
」。

