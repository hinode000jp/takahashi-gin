システム！"dolittle_lib/Studuino/BlockManager" 使う。
システム！"dolittle_lib/Studuino/ST_valueElement" 使う。

「(root:__defineST) == 未定義」！なら「
	root:out		=配列！作る。
	root:bManager	=BlockManager!作る。
	root:stname	="board"。
	root:infix		= 未定義。
	root:__defineST	=1.
」実行。



//　とりあえず

//配列の拡張
配列：まとめる＝「|a|
	「|x| self!(a!(x)読む)書く」！( a！要素数？)　繰り返す。
」。


配列：取得＝「|index|
    o = self!(index) 　読む。
    valEl=未定義.
//    valueEl:dVariableValues!「|_o| 「_o:key == o」!なら「valEl=_o.」実行」それぞれ実行。
//    「valEl != 未定義」!なら「o = _o:name」実行。

o    
」。

stTools = "".
stTools:記号変換＝「|s|
   ret ="？？？？".
  「s==">"」！なら「ret=" > "。」実行。
  「s==">="」！なら「ret=" >= "。」実行。
  「s=="<"」！なら「ret=" < "。」実行。
  「s=="<="」！なら「ret=" <= "。」実行。
  「s=="=="」！なら「ret=" == "。」実行。
  「s=="!="」！なら「ret=" != "。」実行。
  「s=="+"」！なら「ret=" + "。」実行。
  「s=="-"」！なら「ret=" - "。」実行。
  「s=="/"」！なら「ret=" / "。」実行。
  「s=="*"」！なら「ret=" * "。」実行。
  「s=="%"」！なら「ret="  % "。」実行。
  ret
」。

